// const number = [1, 2, 3, 4];
// const strs = number.map((item) => item.toString());
// console.log(strs);

const numbers = [1, 2, 3, false, null, "", 4, undefined];
const filteredNumber = numbers.filter((v) => v);
console.log(filteredNumber);
